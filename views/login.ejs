<!DOCTYPE html>
<html>

<head>
  <title>

  </title>
</head>

<body>
  <header>
    <% include layouts/header %>
  </header>
  <div class="container">

    <form class="form-signin" action="/login" method="POST">
      <h2 class="text-center form-signin-heading">Iniciar sesión</h2>
      <label for="inputEmail" class="sr-only">Email</label>
      <input type="email" required name="email" type="text" id="inputEmail" class="form-control" placeholder="Email address" autofocus="">
      <label for="inputPassword" class="sr-only">Contraseña</label>
      <input type="password" required id="inputPassword" name="password" class="form-control" placeholder="Password">
      <div class="checkbox">
        <label>
              <input type="checkbox" value="remember-me"> Recuerdame
            </label>
      </div>
      <% if ( messages.length > 0 ) { %>
        <%  for(var i in messages){ %>
          <div class="alert alert-danger" role="alert">
            <%= messages[i] %>
          </div>
          <% } %>
            <% } %>

              <button class="btn btn-lg btn-primary btn-block" type="submit">Entrar</button>
              <p>¿Todavía no eres miembro?</p>
              <a href="/registro" class="btn-lg btn-block btn btn-secondary">
                        Nueva cuenta
                      </a>

    </form>
  </div>

</body>

</html>